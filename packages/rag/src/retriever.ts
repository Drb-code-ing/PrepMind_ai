/**
 * 混合检索（向量 + 关键词）
 * TODO: Phase 5 实现 — pgvector hybrid search
 */
export const retriever = {
  search: async (_query: string, _options?: { topK?: number }) => {
    throw new Error('Not implemented');
  },
};
